<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.megalobiz.youtubemockup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SigninActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".SigninFocusedBlankActivity" />
        <activity android:name=".SigninFocusedFilledActivity" />
        <activity android:name=".SigninPasswordBlankActivity" />
        <activity android:name=".SigninPasswordFilledActivity" />
        <activity android:name=".AgreementActivity" />
        <activity android:name=".MusicActivity" />
        <activity android:name=".SideMenuActivity" />
        <activity android:name=".SearchBlankActivity" />
        <activity android:name=".UploadHomeActivity" />
        <activity android:name=".UploadVideosActivity" />
        <activity android:name=".UploadPlaylistsActivity" />
        <activity android:name=".UploadChannelsActivity" />
        <activity android:name=".UploadAboutActivity" />
        <activity android:name=".SearchFilledActivity" />
        <activity android:name=".SearchResultsActivity" />
        <activity android:name=".SearchVideoActivity" />
        <activity android:name=".SearchVideoLikedActivity"></activity>
    </application>

</manifest>